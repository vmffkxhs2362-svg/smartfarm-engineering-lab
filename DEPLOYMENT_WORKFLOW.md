# 🚀 3단계 배포 및 서비스 관리 워크플로우 (Deployment Workflow)

본 문서는 스마트팜 계산기 포털(`HydroCal Pro` 등 관련 웹 자산)의 기획, 테스트, 상용화 과정에서 일반 사용자의 미승인 페이지 유입을 철저히 차단하고 서비스 신뢰성을 확보하기 위한 **3단계 업무 구분 표준 가이드라인**입니다. 에이전트(Antigravity)는 모든 개발 및 Git 작업 시 본 프로세스를 철저히 준수해야 합니다.

---

## 🏛️ 3단계 서비스 라이프사이클

### [1단계] 아이디어 & 기획 (Idea & Draft Phase)
- **개념**: 새로운 비즈니스 모델이나 계산 툴 설계 아이디어를 도출하고 기획 문서를 작성하는 단계.
- **개발 환경 제한**:
  - 로컬 환경(`g:\My Drive\Antigravity\Headquater\...`) 내 마크다운(`*.md`) 파일 형태로 기획 초안을 보관합니다.
  - 해당 단계의 소스코드는 로컬에만 머물러야 하며, **공식 배포 브랜치(`main`)에 커밋하거나 푸시할 수 없습니다.**
  - 이 단계의 메뉴나 탭은 기획용 코드 외에 실제 HTML에 반영되지 않습니다.

### [2단계] 내부 베타 테스트 (Beta Test & Review Phase)
- **개념**: 기획된 코드를 로컬 환경에서 구현하고, 사령관의 내부 품질 검증 및 동선 테스트를 거치는 단계.
- **개발 환경 제한**:
  - **격리 브랜치 사용**: 공식 배포 브랜치(`main`)가 아닌 별도의 테스트용 격리 브랜치(`develop` 또는 `beta`)를 생성하여 작업합니다.
  - **접근 통제**: 일반 사용자가 접속하는 라이브 도메인(GitHub Pages)의 메인 허브(`index.html`)에 링크나 탭 버튼을 노출시킬 수 없으며, 노출이 필요한 경우 아래와 같이 주석처리(`<!-- -->`) 상태로 둡니다.
    ```html
    <!-- [Beta / Under Review]
    <button id="btn-tab-beta-item" onclick="location.href='beta_tool.html'">...</button>
    -->
    ```
  - **로컬 검증**: 에이전트는 로컬 브라우저 구동 또는 격리된 테스트 도메인(예: 로컬 호스트)을 통해서만 검증을 유도하며, 사령관의 검토용 스크린샷이나 동작 로그로만 검증을 보고합니다.

### [3단계] 상용화 공개 (Production Launch Phase)
- **개념**: 내부 테스트가 완료되고, 서비스로서 일반 유저에게 공개하기 적합하다고 판정된 도구를 배포하는 단계.
- **개발 환경 및 배포 승인**:
  - **사령관의 명시적 서면 승인(결재)**이 확인되기 전에는 어떠한 경우에도 `main` 브랜치로의 병합(Merge) 및 원격 저장소로의 푸시(`git push origin main`)를 실행할 수 없습니다.
  - 승인이 완료된 후 배포 브랜치로 머지하여 링크 주석을 해제하고, 최종 릴리즈를 진행합니다.

---

## 🛑 에이전트 행동 강령 및 재결 요건
1. **임의 Push 절대 금지**: 에이전트는 어떠한 자잘한 코드 수정(텍스트 교정, CSS 스타일 정합 등)일지라도 `git push` 명령어 실행 전 사령관에게 수행 동의를 수동으로 구해야 합니다.
2. **핫픽스(예외 조항)**: 상용 배포본에 즉각적인 오류나 오작동, 미승인 노출 사고 발생 시, 유입 차단을 위한 주석 처리 등 핫픽스(Hotfix) 성격의 변경에 대해서는 승인 절차를 최우선으로 유도하되, 사령관에게 즉시 핫픽스 push 여부를 확인받아야 합니다.
